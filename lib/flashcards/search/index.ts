/**
 * lib/flashcards/search/
 * Required by the architecture spec as a top-level folder. Implementation
 * lives in services/search.service.ts for the same reason noted in
 * analytics/index.ts. Re-exported here for the expected import path.
 */
export * from "../services/search.service";
